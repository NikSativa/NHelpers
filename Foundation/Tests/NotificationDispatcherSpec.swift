import Nimble
import NSpry
import Quick

final class NotificationDispatcherSpec: QuickSpec {
    override func spec() {
        describe("NotificationDispatcher") {
            it("should be a real test") {
                expect(true).to(beTrue())
            }
        }
    }
}
