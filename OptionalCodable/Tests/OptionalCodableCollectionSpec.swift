import Nimble
import NSpry
import Quick

@testable import NOptionalCodable
@testable import NOptionalCodableTestHelpers

final class OptionalCodableCollectionSpec: QuickSpec {
    override func spec() {
        describe("OptionalCodableCollection") {
            it("should be a real test") {
                expect(true).to(beTrue())
            }
        }
    }
}
