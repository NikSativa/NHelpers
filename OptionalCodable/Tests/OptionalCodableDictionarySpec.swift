import Nimble
import NSpry
import Quick

@testable import NOptionalCodable
@testable import NOptionalCodableTestHelpers

final class OptionalCodableDictionarySpec: QuickSpec {
    override func spec() {
        describe("OptionalCodableDictionary") {
            it("should be a real test") {
                expect(true).to(beTrue())
            }
        }
    }
}
