import Nimble
import NSpry
import Quick

@testable import NOptionalCodable
@testable import NOptionalCodableTestHelpers

final class OptionalEncodableCollectionSpec: QuickSpec {
    override func spec() {
        describe("OptionalEncodableCollection") {
            it("should be a real test") {
                expect(true).to(beTrue())
            }
        }
    }
}
