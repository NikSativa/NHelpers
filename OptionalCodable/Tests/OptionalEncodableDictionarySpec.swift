import Nimble
import NSpry
import Quick

@testable import NOptionalCodable
@testable import NOptionalCodableTestHelpers

final class OptionalEncodableDictionarySpec: QuickSpec {
    override func spec() {
        describe("OptionalEncodableDictionary") {
            it("should be a real test") {
                expect(true).to(beTrue())
            }
        }
    }
}
